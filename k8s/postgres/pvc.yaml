apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: avaliator
  labels:
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: avaliator
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
